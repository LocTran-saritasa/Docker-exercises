services:
  db:
    build: ./db
    ports:
      - 5432:5432
    environment:
      - POSTGRES_USER=user
      - POSTGRES_PASSWORD=saritasa
      - POSTGRES_DB=auth
    volumes:
      - ./var/pgdata:/var/lib/postgresql/data
