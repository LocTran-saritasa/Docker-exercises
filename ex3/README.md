# [Exercise 3] Database design + graphql

Move to `ex3` directory then run the command below to start the whole application

```bash
docker compose up
```

### Entity relationship diagram (ERD):

![ERD](./erd.png)
